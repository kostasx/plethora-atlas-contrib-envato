// Generated by CoffeeScript 1.10.0
var colors, initCommands;

colors = require('colors');

initCommands = function(program) {
  return program.command('envato').description('Envato related utilities').option('-v, --verify <code>', 'Verify purchase code').action(function(options) {
    var Envato;
    Envato = require('./envato');
    if (options.verify) {
      return Envato.verify({
        verify: options.verify,
        authorName: config.keys.envato.authorName,
        authorAPIKey: config.keys.envato.authorAPIKey
      }).then(function(res) {
        return console.log(res);
      })["catch"](console.log);
    }
  });
};

module.exports = initCommands;
